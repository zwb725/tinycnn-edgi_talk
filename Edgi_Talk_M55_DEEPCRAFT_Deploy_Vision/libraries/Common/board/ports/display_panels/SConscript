from building import *
import os
import rtconfig

cwd  = GetCurrentDir()
path = [cwd]
src  = []

if GetDepend(['COMPONENT_MTB_DISPLAY_tl043wvv02']) or GetDepend(['RT_CHERRYUSB_DEVICE_DISPLAY']):
    src += Glob('display_tl043wvv02.c')
    src += Glob('drv_touch.c')

group = DefineGroup('tl043wvv02-code', src, depend = [''], CPPPATH = path)

Return('group')
