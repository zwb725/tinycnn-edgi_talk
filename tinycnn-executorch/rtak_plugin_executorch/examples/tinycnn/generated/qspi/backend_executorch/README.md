# RT-AK ExecuTorch Backend Prototype

This directory is a target-side interface prototype. It does not contain a completed ExecuTorch Runtime port for PSoC Edge E84. The C functions return explicit not-implemented errors until `RT_AI_EXECUTORCH_RUNTIME_READY` is enabled and a real runtime integration is added.
